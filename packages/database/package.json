{
	"name": "@package/database",
	"version": "0.0.0",
	"main": "./index.ts",
	"types": "./index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"db:generate": "pnpm prisma generate dev",
		"db:push": "pnpm prisma db push --skip-generate"
	},
	"keywords": [],
	"author": "",
	"devDependencies": {
		"prisma": "^5.9.1"
	},
	"dependencies": {
		"@prisma/client": "^5.9.1"
	}
}
